# MangaVega Tracker V7 — Variables d'environnement
# 
# Copiez ce fichier en .env et remplissez les valeurs :
#   cp .env.example .env        (Linux/Mac)
#   copy .env.example .env      (Windows)
#
# Le fichier .env n'est JAMAIS commité (il est dans .gitignore)

# Token GitHub (scopes: gist + repo + workflow)
# Créez-le sur https://github.com/settings/tokens
GIST_TOKEN=
EMAIL_DESTINATAIRE=
EMAIL_EXPEDITEUR=
MOT_DE_PASSE_APP=